function greet(name: string): string {
  console.log("Greeting invoked");
  const prefix = "Hello";
  return prefix + " " + name;
}

function add(a: number, b: number): number {
    const result = a + b;
  return a + b;
}